#include <stdio.h>
#include "descriptografar.h"
#include "interface.h"

void descriptografar() {
	int p, q, e, d;
	i_descriptografar(1);
	i_descriptografar(2);
	scanf("%d", &p);
	i_descriptografar(3);
	scanf("%d", &q);
	i_descriptografar(4);
	scanf("%d", &e);
	i_descriptografar(5);
}
